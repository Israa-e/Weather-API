services:
  weather-api:
    build: .
    container_name: weather-api
    ports:
      - "3000:3000"
    env_file:
      - .env
    depends_on:
      - redis

  redis:
    image: redis:7-alpine
    container_name: weather-redis
    ports:
      - "6379:6379"
